# 歌词智能分析 - 项目备份文件

## 📦 备份文件信息

**文件名称**: lyrics-analyzer-backup.tar.gz
**文件大小**: 769.59 KB
**上传时间**: 2026-02-07
**有效期**: 30天

## 🔗 下载地址

**[点击下载备份文件](https://coze-coding-project.tos.coze.site/coze_storage_7604092844659245094/lyrics-analyzer-backup.tar_3d63bbbf.gz?sign=1773060969-3c13913e73-0-dd721aa8c236f2cd526a6b6442d6cc05c817721f7f84244677201cdeea5d4877)**

**注意**: 下载链接有效期为30天，请及时下载保存。

## 📋 备份内容说明

备份文件包含以下内容：

### ✅ 已包含
- **源代码** (`src/`) - 完整的前端和后端代码
- **配置文件** (`*.config.ts`, `*.json`) - 所有配置
- **Android项目** (`android/`) - Capacitor Android项目配置
- **文档** (`*.md`) - 所有说明文档
- **脚本** (`scripts/`) - 构建和运行脚本
- **静态资源** (`public/`) - 图片、图标等

### ❌ 已排除
- `node_modules/` - 依赖包（安装时自动下载）
- `.next/` - 构建缓存
- `out/` - 静态构建输出（运行build命令生成）
- `android/app/build/` - Android构建产物
- `.git/` - Git版本控制
- `*.log` - 日志文件
- `*.apk` - APK文件（需单独构建）
- `*.keystore`, `*.jks` - 签名密钥（敏感信息）

## 🚀 如何使用备份文件

### 方法一：完整恢复项目

```bash
# 1. 解压缩
tar -xzf lyrics-analyzer-backup.tar.gz

# 2. 进入项目目录
cd projects

# 3. 安装依赖
pnpm install

# 4. 启动开发服务器
pnpm run dev

# 访问 http://localhost:5000
```

### 方法二：仅查看源代码

```bash
# 解压即可查看所有源代码
tar -xzf lyrics-analyzer-backup.tar.gz
cd projects/src
```

### 方法三：重新构建APK

```bash
# 1. 解压缩
tar -xzf lyrics-analyzer-backup.tar.gz
cd projects

# 2. 安装依赖
pnpm install

# 3. 构建前端
pnpm run build

# 4. 同步到Android
pnpm run sync:android

# 5. 构建APK
cd android
./gradlew assembleDebug
```

详细构建步骤请参考 `BUILD_APK.md` 文档。

## 📁 项目结构

```
projects/
├── src/                          # 源代码
│   ├── app/
│   │   ├── api/lyrics/           # API路由
│   │   ├── page.tsx              # 主页面
│   │   ├── layout.tsx            # 布局
│   │   └── robots.ts             # Robots配置
│   └── components/ui/            # UI组件
│
├── android/                      # Android项目
│   ├── app/
│   │   ├── build.gradle
│   │   └── src/main/
│   └── gradlew                   # Gradle Wrapper
│
├── scripts/                      # 脚本文件
│   ├── build-android.sh          # APK构建脚本
│   ├── sync-android.sh           # 同步脚本
│   ├── upload-backup.ts          # 备份上传脚本
│   ├── build.sh
│   ├── dev.sh
│   └── start.sh
│
├── out/                          # 静态构建（运行build后生成）
├── public/                       # 静态资源
├── capacitor.config.ts           # Capacitor配置
├── next.config.ts                # Next.js配置
├── package.json                  # 项目依赖
├── tsconfig.json                 # TypeScript配置
│
├── BUILD_APK.md                  # APK构建完整指南
├── QUICKSTART_APK.md             # APK快速入门
├── GET_APK.md                    # APK获取方案
├── NO_APK_AVAILABLE.md           # APK限制说明
├── ACCESS_GUIDE.md               # 访问指南
└── BACKUP_INFO.md                # 本文档
```

## 🔧 技术栈

- **前端**: Next.js 16 + React 19 + TypeScript 5
- **UI**: shadcn/ui + Tailwind CSS 4
- **移动端**: Capacitor 8
- **后端**: Node.js + Next.js API Routes
- **AI**: coze-coding-dev-sdk (LLM集成)

## ✨ 功能列表

1. **歌词结构拆解**
   - 分析歌词结构
   - 韵脚分析
   - 句式特点
   - 修辞手法
   - 情感脉络

2. **爆款歌词分析**
   - 主题定位
   - 情感策略
   - 语言特点
   - 结构技巧
   - 传播要素

3. **意象词生成**
   - 自然意象
   - 时间意象
   - 物品意象
   - 动作意象
   - 颜色意象
   - 感官意象
   - 抽象意象

## ⚠️ 重要提示

### 关于API配置

当前应用使用后端API调用LLM。如果您将此项目部署到生产环境：

1. **部署API到云端**
   - 将 `src/app/api/lyrics/` 部署到服务器
   - 推荐平台：Vercel、Railway、AWS Lambda等

2. **修改API地址**

   在 `src/app/page.tsx` 中配置API基础URL：

   ```typescript
   const API_BASE_URL = 'https://your-api-server.com';

   const response = await fetch(`${API_BASE_URL}/api/lyrics/${type}`, {
     method: 'POST',
     headers: { 'Content-Type': 'application/json' },
     body: JSON.stringify({ lyrics: input }),
   });
   ```

### 关于依赖安装

备份文件不包含 `node_modules/`，第一次使用时需要安装依赖：

```bash
pnpm install
```

### 关于环境变量

项目可能需要配置环境变量（如对象存储、API密钥等），请根据实际需求配置 `.env` 文件。

## 📞 需要帮助？

如果在使用备份文件时遇到问题：

1. 查看项目文档：
   - `BUILD_APK.md` - APK构建
   - `QUICKSTART_APK.md` - 快速入门
   - `ACCESS_GUIDE.md` - 访问指南

2. 检查日志：
   ```bash
   tail -f /app/work/logs/bypass/app.log
   ```

3. 常见问题：
   - 构建失败 → 检查 Node.js 版本（需要 18+）
   - 依赖安装失败 → 尝试 `rm -rf node_modules pnpm install`
   - API调用失败 → 检查API地址配置

## 🔄 重新生成备份

如果需要重新生成备份文件，可以运行：

```bash
# 打包
tar -czf lyrics-analyzer-backup.tar.gz \
  --exclude='node_modules' \
  --exclude='.next' \
  --exclude='out' \
  --exclude='.git' \
  --exclude='android/app/build' \
  --exclude='android/.gradle' \
  --exclude='*.log' \
  --exclude='*.apk' \
  .

# 上传
npx tsx scripts/upload-backup.ts
```

## 📅 备份策略建议

建议定期备份：
- **开发阶段**：每周备份一次
- **重大修改前**：手动备份
- **发布前**：创建版本快照

## 🎉 总结

此备份文件包含：
- ✅ 完整的项目源代码
- ✅ 所有配置文件
- ✅ Android项目配置
- ✅ 详细的使用文档

您可以：
- ✅ 在任何环境中恢复项目
- ✅ 查看和学习代码
- ✅ 重新构建APK
- ✅ 部署到生产环境

---

**祝您使用愉快！如有问题请参考文档或寻求技术支持。** 🚀
